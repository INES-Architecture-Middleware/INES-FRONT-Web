import { useIntl } from "react-intl";
import './Home.scss'

const Home = (props) => {
    const intl = useIntl()

    return (
        <div className="Home">
            HOME
        </div>
    )
}

export default Home